1. Run redis-server.exe to start the Redis server.
2. Open a new Command Prompt and run redis-cli.exe to test the connection.(127.0.0.1:6379> if you get this in CMD then it is working)
3. redis-cli.exe shutdown (command to stop redis server)
